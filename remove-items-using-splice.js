const arr = [2, 4, 5, 1, 7, 5, 2, 1];
// Only change code below this line
arr.splice(1,4); //O primeiro parâmetro representa o índice na matriz a partir do qual começar a remover elementos, enquanto o segundo parâmetro indica o número de elementos a serem excluídos.
// Only change code above this line
console.log(arr);